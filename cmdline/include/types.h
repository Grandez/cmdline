#pragma once
/*
#include <unordered_map>

using namespace std;

namespace cmdline_ {

#ifndef __CMDLINE_TYPES__
#define __CMDLINE_TYPES__
	enum class Type { FLAG, STRING, NUMBER, DECIMAL, DATE, TIME, DATETIME, TMS, DIR, DIR_EXISTS, FILE, FILE_EXISTS };
	enum class Source { DEFAULT, ENV, CMDLINE, AUTO };
	//	    typedef unordered_map<string, Argument>  Args;
	typedef std::unordered_map<std::string, bool>      Flags;
	typedef pair<std::string, bool>               Flag;
	typedef std::unordered_map<std::string, std::string>    Options;
	typedef pair<std::string, std::string>             Option;
	typedef unordered_map<std::string, vector<std::string>> Definitions;

   #define strdup(a) myStrdup(a)
#endif
}
*/